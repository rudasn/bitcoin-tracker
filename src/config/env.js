// Extract global env variables.

const env = {
  NODE_ENV: process.env.NODE_ENV,
  VERSION: process.env.REACT_APP_VERSION,
  NAME: process.env.REACT_APP_NAME,
  PUBLIC_URL: process.env.PUBLIC_URL,
}

export default env
